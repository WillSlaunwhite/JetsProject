package jets;

public class JetsImpl extends Jets {
	public void newJetImpl(String model, double speed, int range, long price) {
		this.setModel(model);
		this.setSpeed(speed);
		this.setRange(range);
		this.setPrice(price);
	}
	
	
}
